package main

import (
  "encoding/json"
  "fmt"
  "net/http"
)

{{> snippets/import}}

func globalFlapjackUserID() {
  client, err := search.NewClient("YourApplicationID", "YourAdminAPIKey")
  if err != nil {
    fmt.Println(err)
  }

  client.AddDefaultHeader("X-Flapjack-UserToken", "test-user-123")
}
