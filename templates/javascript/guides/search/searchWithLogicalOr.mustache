{{> snippets/import}}
import type { SearchParamsObject, OptionalWords } from 'flapjack-search';

{{> snippets/init}}
const query = 'the query';
const optionalWords: OptionalWords = ['the', 'query'];
const searchParams: SearchParamsObject = {
  query,
  optionalWords
};
{{#dynamicSnippet}}searchWithSearchParams{{/dynamicSnippet}};
