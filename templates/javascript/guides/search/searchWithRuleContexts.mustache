{{> snippets/import}}
import type { SearchParams } from 'flapjack-search';

const getPlatformTag = () => {
  return ""; // Implement your logic here
}

{{> snippets/init}}

const platformTag = getPlatformTag();
const searchParams: SearchParams = {
  query: "<YOUR_SEARCH_QUERY>",
  ruleContexts: [platformTag]
};

{{#dynamicSnippet}}searchWithSearchParams{{/dynamicSnippet}}
