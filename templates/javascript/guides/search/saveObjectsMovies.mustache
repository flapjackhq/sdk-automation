{{> snippets/import}}

{{> snippets/init}}

// Fetch and index objects in Flapjack
const processRecords = async () => {
  const datasetRequest = await fetch('https://dashboard.flapjack.com/api/1/sample_datasets?type=movie');
  const movies = await datasetRequest.json();
  return {{#dynamicSnippet}}saveObjectsMovies{{/dynamicSnippet}}
};

processRecords()
  .then(() => console.log('Successfully indexed objects!'))
  .catch((err) => console.error(err));
