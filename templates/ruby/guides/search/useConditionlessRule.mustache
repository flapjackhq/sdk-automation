{{> snippets/import}}

{{> snippets/init}}

object_id = "a-rule-id"

rule = Flapjack::Search::Rule.new(
  object_id: object_id,
  consequence: Flapjack::Search::Consequence.new(
    # Set relevant consequence
  ),
)

# Set validity (optional)
rule.validity = [
  Flapjack::Search::TimeRange.new(
    from: 1688774400,
    until: 1738972800,
  ),
]

{{#dynamicSnippet}}saveRule{{/dynamicSnippet}}
