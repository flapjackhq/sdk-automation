{{> snippets/import}}
import com.flapjackhq.client.model.search.*

suspend fun globalFlapjackUserID() {
  val client = SearchClient(
    appId = "FLAPJACK_APPLICATION_ID",
    apiKey = "FLAPJACK_API_KEY",
    options = ClientOptions(defaultHeaders = mapOf("X-Flapjack-User-ID" to "test-user-123"))
  )
  println(client.options.defaultHeaders)
}
